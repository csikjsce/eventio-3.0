name: Build Images (legacy)

# Superseded by deploy-production.yml (builds SHA-tagged images after deploy).
# Kept disabled to avoid duplicate pushes.

on:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  noop:
    name: Disabled — use Deploy Production
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use workflow Deploy Production for image builds + deploy"
